name: Spring Boot CI/CD

on:
  push:
    branches:
      - "**"
jobs:
  pipeline:
    uses: projet-synthese-gi26/workflows/.github/workflows/django-ci-cd-v2.yml@main
    with:
      app_name: social-boost
    secrets: inherit
